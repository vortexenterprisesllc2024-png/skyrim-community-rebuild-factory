#pragma once

namespace AdventureXP
{
    // Zero vanilla skill-rank contribution to player level when configured.
    // Uses the documented game setting fXPPerSkillRank (not a code hook).
    void ApplySkillLevelingSetting();
}
